import theme from '../../theme';

const styles: { [key: string]: React.CSSProperties } = {
  toolbarInnerDiv: {
    width: '100%',
    display: 'flex',
    flexDirection: 'row',
  },
  brandingDiv: {
    flexGrow: 1,
  },
};
export default styles;
