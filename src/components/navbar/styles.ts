import theme from '../../theme';

const styles: { [key: string]: React.CSSProperties } = {
  linksContainer: {},
  hyperlink: {
    textDecoration: 'none',
    color: 'white',
  },
};
export default styles;
